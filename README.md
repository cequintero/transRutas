
  # TransRutas Bogotá UI Prototype

  This is a code bundle for TransRutas Bogotá UI Prototype. The original project is available at https://www.figma.com/design/YniUxzlhKVfH8IcYg21alf/TransRutas-Bogot%C3%A1-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  